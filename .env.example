# Copy to .env and fill in for docker-compose, or export these directly
# when running the backend without Docker.

POSTGRES_PASSWORD=change_me_in_production
JWT_SECRET_KEY=change_me_in_production

# Optional: enables real LLM-backed AI Assistant responses.
# Leave unset to use the rule-based assistant (fully functional without it).
ANTHROPIC_API_KEY=
